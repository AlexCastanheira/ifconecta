<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Esqueci Minha Senha - If(Conecta)</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <%- include('partials/header') %>

        <div class="auth-container">
            <div class="auth-card">
                <h2>Esqueci Minha Senha</h2>

                <% if (error) { %>
                    <div class="error-message">
                        <%= error %>
                    </div>
                    <% } %>

                        <p class="auth-description">Digite seu e-mail abaixo para receber um link de redefinição de
                            senha.</p>

                        <form action="/auth/forgot-password" method="POST">
                            <div class="form-group">
                                <label for="email">E-mail</label>
                                <input type="email" id="email" name="email" required>
                            </div>

                            <button type="submit" class="btn primary-btn">Enviar Link de Redefinição</button>
                        </form>

                        <div class="auth-links">
                            <p>Lembrou sua senha? <a href="/auth/login">Voltar para o Login</a></p>
                        </div>
            </div>
        </div>

        <%- include('partials/footer') %>

            <script src="/js/main.js"></script>
</body>

</html>